# Provider Calendar - Unit Test Mapping

## AllDayHolidayRow-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 1 | | renders empty cell for each provider on page | In Day view, all-day holiday row renders empty cells when the selected date is not a holiday. | Render CalendarPageContainer with date 2024-12-18 (non-holiday) and 4 providers -> assert 4 all-day cells exist -> verify all cells have empty text content. | Verifies holiday row renders empty cells for non-holiday dates in Day view. | Day view all-day row. In scope: empty cell rendering per provider. Out of scope: holiday content; navigation. |
| 2 | | renders cell with holiday for each provider on page | In Day view, all-day holiday row shows holiday name when the selected date is a holiday. | Render CalendarPageContainer with date 2024-12-25 (Christmas Day) and 4 providers -> assert all-day cells display "Christmas Day". | Verifies holiday row shows holiday name on holiday dates. | Day view holiday display. In scope: holiday text rendering. Out of scope: navigation; non-holiday dates. |
| 3 | | responds to date navigation - forward button (Day) | Day view forward navigation updates holiday row content as dates change. | Render with date 2024-11-27 -> click forward once -> assert cells show "Thanksgiving Day" -> click forward again -> assert cells are empty. | Forward navigation in Day view updates holiday row correctly across holiday/non-holiday boundaries. | Day view forward navigation + holiday row. In scope: forward button; holiday text update. Out of scope: back button; week view. |
| 4 | | responds to date navigation - back button (Day) | Day view back navigation updates holiday row content. | Render with date 2024-09-03 -> click back once -> assert cells show "Labor Day" -> click back again -> assert cells are empty. | Back navigation in Day view updates holiday row correctly. | Day view back navigation + holiday row. In scope: back button; holiday text update. Out of scope: forward button; week view. |
| 5 | | responds to date navigation - today button (Day) | Today button returns to current date and clears holiday content. | Render with date 2024-09-05 -> navigate back 3 days (hitting Labor Day) -> click "Today" -> assert cells are empty again. | Today button resets to current date and updates holiday row. | Day view Today button + holiday row. In scope: today button; holiday reset. Out of scope: forward/back; week view. |
| 6 | | renders empty cell for each day of the week (Week) | In Week view, all-day row renders 7 empty cells on a non-holiday week. | Render with date 2024-12-18 -> switch to Week view -> assert 7 all-day cells exist -> verify all cells are empty. | Verifies Week view holiday row renders 7 empty cells for non-holiday weeks. | Week view all-day row. In scope: 7 empty cells. Out of scope: holiday content; Day view. |
| 7 | | renders cells with holiday (Week) | In Week view, holiday cells display the correct holiday name at the correct day index. | Render with date 2024-12-25 -> switch to Week view -> assert 7 cells -> verify index 2 = "Christmas Eve" and index 3 = "Christmas Day" -> verify remaining cells are empty. | Verifies Week view correctly places holiday names at the right day of the week. | Week view holiday placement. In scope: holiday name at correct index. Out of scope: navigation; Day view. |
| 8 | | responds to date navigation - forward button (Week) | Week view forward navigation updates holiday cells across weeks. | Render with date 2024-05-22 in Week view -> navigate forward once -> assert index 1 = "Memorial Day" -> navigate forward again -> assert all cells empty. | Forward navigation in Week view correctly updates holiday row across week boundaries. | Week view forward navigation + holiday row. In scope: forward button; holiday placement per week. Out of scope: back button; Day view. |
| 9 | | responds to date navigation - back button (Week) | Week view back navigation updates holiday cells. | Render with date 2025-01-07 in Week view -> navigate back once to New Year's week -> navigate back again to Christmas week -> verify holiday names at correct indices. | Back navigation in Week view correctly shows holidays for each navigated week. | Week view back navigation + holiday row. In scope: back button; multi-week holiday check. Out of scope: forward; Day view. |
| 10 | | responds to date navigation - today button (Week) | Today button in Week view returns to current week and clears holiday content. | Render with date 2024-07-24 in Week view -> navigate back 3 weeks (hitting Independence Day at index 4) -> click "Today" -> assert all cells empty. | Today button resets to current week in Week view and clears holiday display. | Week view Today button + holiday row. In scope: today reset; holiday clearance. Out of scope: Day view; forward/back. |

## AppointmentCard-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 11 | | should open appointment card when block is clicked and close when clicked again | Toggle behavior of appointment card flyout on block click. | Render CalendarPageContainer -> click an appointment block -> assert flyout appears -> click same block again -> assert flyout disappears. | Appointment card flyout toggles open/closed on repeated clicks of the same block. | Appointment card toggle. In scope: open/close toggle on same block. Out of scope: different block switching; outside clicks. |
| 12 | | should switch to a different appointment card when a different block is clicked | Clicking a different appointment block switches the flyout to that appointment. | Render -> click first appointment block -> verify setSelectedAppointment called with appointment_1 -> click second block -> verify called with appointment_2 -> assert flyout remains open. | Clicking a different appointment block switches the selected appointment without closing the flyout. | Appointment card switching. In scope: switching between blocks; flyout stays open. Out of scope: toggle; outside clicks. |
| 13 | | should handle outside clicks correctly | Clicking outside the appointment card closes the flyout. | Render -> click appointment block -> assert flyout open -> click date header (outside area) -> assert flyout closes. | Outside click correctly dismisses the appointment card flyout. | Outside click dismissal. In scope: flyout close on outside click. Out of scope: toggle; block switching. |
| 14 | | should pass approved providers or resources only to appointment card | AppointmentCard receives only approved providers. | Render -> click appointment block -> inspect props passed to mocked AppointmentCard component -> assert only 2 approved providers with specific IDs are passed. | Only approved providers are passed to the AppointmentCard component, filtering out unapproved ones. | Provider filtering for appointment card. In scope: approved provider prop filtering. Out of scope: card UI; interactions. |
| 14a | | should render AppointmentCard instead of LegacyAppointmentCard | When the connected-card AB flag is on, the new AppointmentCard renders instead of the legacy one. | Render CalendarPageContainer with `ProviderInterface_UseConnectedAppointmentCard: 'on'` -> click an appointment block -> assert appointment-details-flyout visible -> assert mockAppointmentCard called and mockLegacyAppointmentCard not called. | Connected AppointmentCard is rendered (and legacy bypassed) when the AB flag is enabled. | AB flag gating for AppointmentCard. In scope: flag-on rendering of new card. Out of scope: flag-off path; legacy card behavior. |
| 14b | | should open and close appointment card on repeated clicks | With the connected-card flag on, clicking the same block toggles the flyout open/closed. | Render with connected-card flag on -> click appointment block -> assert flyout open -> click block again -> assert flyout closed. | Connected card preserves toggle-on-same-block behavior of the legacy card. | Connected AppointmentCard toggle. In scope: open/close toggle. Out of scope: outside click; switching blocks; flag-off. |
| 14c | | should switch between appointments when different blocks are clicked | With the flag on, clicking a different block updates the selected appointment without closing the flyout. | Spy on `useSelectedAppointment.setSelectedAppointment` -> render with flag on -> click block 1 -> assert spy called with appointment_1 -> click block 2 -> assert spy called with appointment_2 -> assert flyout still open. | Connected card switches selected appointment between blocks while flyout stays open. | Connected AppointmentCard switching. In scope: setSelectedAppointment calls; flyout persistence. Out of scope: outside click; flag-off. |
| 14d | | should close appointment card on outside click | With the flag on, clicking the date header (outside region) dismisses the flyout. | Render with flag on -> click appointment block -> assert flyout open -> click date-header -> assert flyout closed. | Connected card closes the flyout on outside clicks. | Connected AppointmentCard outside click. In scope: outside dismissal. Out of scope: toggle; switching; flag-off. |

## BlockTypeFilters-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 15 | | loads filters from previous session | Block type filter state is restored from localStorage on mount. | Seed localStorage with ['availability'] -> render CalendarPageContainer -> assert appointments checkbox is unchecked -> assert availability checkbox is checked. | Block type filters correctly load persisted state from localStorage. | Filter persistence load. In scope: localStorage restoration; checkbox state. Out of scope: saving; block visibility. |
| 16 | | selects all filters if none found from previous session | When no localStorage entry exists, both filters default to checked. | Clear localStorage -> render -> assert both appointments and availability checkboxes are checked. | Both block type filters default to selected when no prior session data exists. | Default filter state. In scope: default checked state. Out of scope: persistence; interaction. |
| 17 | | shows and hides availability blocks | Toggling the availability filter checkbox shows/hides availability blocks on the calendar. | Render with date 2024-09-10 -> assert availability blocks for slot_1 and slot_2 visible -> click availability checkbox to uncheck -> assert blocks disappear -> click again to recheck -> assert blocks reappear. | Availability checkbox toggle controls visibility of availability blocks on the calendar grid. | Availability filter toggle. In scope: checkbox interaction; block show/hide. Out of scope: appointments filter; persistence. |

## CalendarPage-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 18 | | shows the loading page if the data return is loading | Loading state renders the loading page. | Mock data hook to return loading: true -> render CalendarPageContainer -> assert loading-page test ID is present. | Calendar shows loading page when data is still fetching. | Loading state. In scope: loading page display. Out of scope: error states; loaded state. |
| 19 | | redirect users if the gql call is unauthorized | Unauthorized GQL error redirects to sign-in page. | Mock data hook to return AUTH_NOT_AUTHENTICATED error -> render -> assert window.location.href contains /signin?ReturnUrl. | Unauthorized users are redirected to the sign-in page with a return URL. | Auth redirect. In scope: redirect URL with ReturnUrl param. Out of scope: other error types; loading. |
| 20 | | shows the general error page if gql call is failed | Non-auth GQL errors render the error page. | Mock data hook to return a non-auth GQL error -> render -> assert calendar-error-page test ID is present. | General GQL errors display the calendar error page. | Error page display. In scope: error page for non-auth errors. Out of scope: auth errors; loading. |
| 21 | | fires view metric when calendar grid is rendered | A V2 View metric is sent on calendar grid render with correct metadata. | Render with 2 providers and 3 locations -> assert V2 "Calendar grid" View metric sent with num_provider_columns, provider_ids, location_ids, day_of_view, selected_block_types, and view_type. | Calendar grid render fires a View metric with provider, location, and view state metadata. | Metrics on render. In scope: metric name; all metadata fields. Out of scope: click metrics; sidebar metrics. |
| 22 | | does not render MPLDropdown for internal users | MPLDropdown is hidden for internal users. | Mock user as is_internal_user: true -> render -> assert "MPL Dropdown" text is not in the document. | MPLDropdown is not rendered for internal users regardless of feature flag. | MPLDropdown internal user gate. In scope: internal user check. Out of scope: external users; feature flag. |
| 23 | | does not render MPLDropdown when feature flag is disabled | MPLDropdown is hidden when feature flag is off. | Mock feature flag selector as false -> render -> assert "MPL Dropdown" text is not in the document. | MPLDropdown is not rendered when its feature flag is disabled. | MPLDropdown feature flag gate. In scope: flag disabled. Out of scope: internal users; flag enabled. |
| 24 | | renders MPLDropdown for external users when feature flag is enabled | MPLDropdown renders for external users with feature flag on. | Mock external user and feature flag as true -> render -> assert "MPL Dropdown" text is in the document. | MPLDropdown renders when the user is external and the feature flag is enabled. | MPLDropdown enabled state. In scope: external user + flag on. Out of scope: internal users; flag off. |
| 25 | | toggles sidebar | Sidebar visibility toggles and fires metrics on click. | Render -> verify sidebar visible with aria-label "Hide filters" -> click toggle -> assert sidebar hidden with "Show filters" -> click again -> assert visible again -> verify V2 metrics for view and click events. | Sidebar toggle button correctly shows/hides sidebar and fires analytics metrics. | Sidebar toggle + metrics. In scope: visibility toggle; aria-labels; V2 metrics. Out of scope: persistence; filter content. |
| 26 | | show sidebar if previously visible | Sidebar loads as visible from localStorage preference. | Seed localStorage zd_calendar_sidebar_open_preference = 'true' -> render -> assert sidebar visible -> assert toggle says "Hide filters". | Sidebar visibility is restored as visible from localStorage. | Sidebar persistence (visible). In scope: localStorage read; visible state. Out of scope: hidden state; toggle. |
| 27 | | hides sidebar if previously hidden | Sidebar loads as hidden from localStorage preference. | Seed localStorage with 'false' -> render -> assert sidebar not visible -> assert toggle says "Show filters". | Sidebar visibility is restored as hidden from localStorage. | Sidebar persistence (hidden). In scope: localStorage read; hidden state. Out of scope: visible state; toggle. |
| 28 | | calls setQueryVariables with utc timestamp | Calendar data query is initialized with correct UTC timestamp and sorted IDs. | Render with 2 providers and 3 locations -> assert setQueryVariables called with UTC midnight timestamp for America/New_York (2015-09-10T04:00:00+00:00), periodType: 'DAY', and sorted provider/location IDs. | Calendar data query receives correctly formatted UTC timestamp and sorted provider/location IDs. | Query variable initialization. In scope: UTC conversion; periodType; sorted IDs. Out of scope: response handling; error states. |

## DateHeaderNav-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 29 | | page defaults to day view when no timeframe preference is set | Default view is Day when no localStorage preference exists. | Render CalendarPageContainer -> assert Day radio checked, Week unchecked -> assert date header shows "Sep 10, 2024" -> assert no week-header test ID. | Calendar defaults to Day view with no timeframe preference stored. | Default Day view. In scope: radio state; header format; no week-header. Out of scope: Week view; localStorage. |
| 30 | | page retrieves timeframe view from localStorage | View mode is restored from localStorage. | Seed localStorage with 'week' -> render -> assert Week radio checked -> assert date header shows "Sep 2024" -> assert week-header present. | Calendar restores Week view from localStorage preference. | View mode persistence. In scope: localStorage read; Week view state. Out of scope: Day view; saving. |
| 31 | | week button switches to week view and saves preference in localStorage | Clicking Week radio switches view and persists preference. | Render -> click Week radio -> assert Week checked -> assert localStorage set to 'week' -> assert header shows "Sep 2024" -> assert week-header present. | Week button switches to Week view and saves preference to localStorage. | Week view switch + persistence. In scope: radio click; localStorage write; header update. Out of scope: Day switch; navigation. |
| 32 | | day button switches to day view | Clicking Day radio after being in Week view returns to Day view. | Render -> click Week -> click Day -> assert date header shows "Sep 10, 2024" -> assert no week-header. | Day button switches back from Week view to Day view. | Day view switch. In scope: Day radio click; header format. Out of scope: persistence; navigation. |
| 33 | | navigates back and forward by day at beginning of month | Day navigation at month start crosses month boundaries correctly. | Render with Sep 1 -> back twice to Aug 30 -> forward twice back to Sep 1 -> verify header text and calendar-title month updates at each step. | Day navigation correctly crosses month boundaries at the beginning of a month. | Day nav month boundary (start). In scope: back/forward; month transition; header updates. Out of scope: Week view; mid/end month. |
| 34 | | navigates back and forward by day in middle of month | Day navigation in mid-month works correctly. | Render with Sep 10 -> back twice to Sep 8 -> forward twice back to Sep 10 -> verify header text. | Day navigation works correctly in the middle of a month. | Day nav mid-month. In scope: back/forward; header updates. Out of scope: month boundaries. |
| 35 | | navigates back and forward by day at end of month | Day navigation at month end crosses into next month correctly. | Render with Sep 30 -> forward to Oct 1, Oct 2 -> back to Oct 1, Sep 30 -> verify month transitions in calendar-title. | Day navigation correctly crosses month boundaries at the end of a month. | Day nav month boundary (end). In scope: forward/back; month transition. Out of scope: Week view; beginning of month. |
| 36 | | navigates back and forward by week at beginning of month | Week navigation at month start crosses month boundaries. | Render with Sep 1 in Week view -> back twice -> forward twice -> verify correct week start dates (data-test IDs) and header month text. | Week navigation correctly handles month boundaries at beginning of month. | Week nav month boundary (start). In scope: week back/forward; week start dates; month text. Out of scope: Day view. |
| 37 | | navigates back and forward by week in middle of month | Week navigation mid-month works correctly. | Render with Sep 10 in Week view -> back/forward through week boundaries -> verify week start dates. | Week navigation works correctly in mid-month. | Week nav mid-month. In scope: week back/forward. Out of scope: month boundaries. |
| 38 | | navigates back and forward by week at end of month | Week navigation at month end shows cross-month header. | Render with Sep 30 in Week view -> verify cross-month header text (e.g., "Sep - Oct 2024") and correct week start dates. | Week navigation at month end correctly displays cross-month headers. | Week nav month boundary (end). In scope: cross-month header; week dates. Out of scope: Day view; beginning of month. |
| 39 | | navigates to current day in day view | Today button returns to current date in Day view. | Render with Sep 1 -> back twice to Aug 30 -> click Today -> assert back to Sep 1 with correct calendar-title. | Today button navigates back to the current day in Day view. | Today button (Day). In scope: today click; date reset. Out of scope: Week view. |
| 40 | | navigates to current week in week view | Today button returns to current week in Week view. | Render with Sep 1 in Week view -> back twice to August -> click Today -> assert week starting Sep 1 shown. | Today button navigates back to the current week in Week view. | Today button (Week). In scope: today click; week reset. Out of scope: Day view. |

## FullPageCalendarGrid-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 41 | | shows a restricted calendar grid | Calendar grid restricts visible time slots to business hours. | Render with date 2025-06-20 12:00 ET -> assert 00:00 and 06:00 not in DOM -> assert 07:00 present -> assert 19:00 present -> assert 20:00 and 23:00 not rendered. | Calendar grid only renders business-hour time slots, hiding early morning and late evening slots. | Time slot restriction. In scope: business hours boundary (07:00-19:00). Out of scope: timezone adjustment. |
| 42 | | adjusts the restricted calendar grid when changing the timezone | Grid adjusts visible hours when timezone is changed. | Render with date 2025-06-20 and an exclusion at 09:00-10:00 UTC (05:00-06:00 ET) -> verify grid starts at 05:00 ET -> switch timezone to Central (Chicago) -> verify grid shifts to start at 04:00 CT. | Calendar grid adjusts its visible time window when the user changes the display timezone. | Timezone-based grid adjustment. In scope: grid start time shift per timezone. Out of scope: business hours without exclusions. |

## HomePage-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 43 | | should render the patient name page if see all patients are clicked | Full patient search flow: dropdown shows first 3 results, "See all" navigates to results page, "Back to Calendar" returns. | Render HomePage -> type in patient search field -> run debounce timers -> assert only first 3 patients in dropdown -> click "See all results" -> assert calendar replaced by patient results page with all 4 patients -> click "Back to Calendar" -> verify calendar returns -> verify navbar rendered correctly throughout. | Patient search dropdown limits to 3 results; "See all" shows full results page; "Back to Calendar" navigates back. | Patient search end-to-end flow. In scope: search input; dropdown limit; see all; results page; back navigation; navbar. Out of scope: patient record redirect. |
| 44 | | should redirect to patient record page when clicking on a row in the patient results page | Clicking a patient row redirects to the patient record URL. | Navigate to patient results page -> click a patient row -> assert window.location.href navigates to /provider/pt_1/patient/0-1/record. | Clicking a patient row in the results page redirects to the correct patient record URL. | Patient record redirect. In scope: row click; URL redirect. Out of scope: search flow; record page content. |
| 45 | | renders the navbar when displaying the calendar page | Navbar renders on the main calendar view. | Render HomePage -> assert navbar is rendered correctly on main calendar view. | Navbar is always present on the calendar page. | Navbar rendering (calendar). In scope: navbar presence. Out of scope: patient search; error states. |
| 46 | | renders the navbar if calendar grid errors | Navbar still renders when the calendar has a GQL error. | Mock GQL error -> render -> assert navbar rendered correctly -> assert calendar-error-page test ID present. | Navbar remains visible even when the calendar encounters an error. | Navbar rendering (error state). In scope: navbar with error; error page display. Out of scope: loading state. |
| 46a | | logs practice id on page view | A V2 page-view event is sent on HomePage render with the current practice id and the ProviderCalendarWebApp application. | Render HomePage -> assert `sendV2PageViewEvent` called with `application: 'ProviderCalendarWebApp'`, `additionalFields.page_type: 'Provider'`, `current_user_practice_id: 'pt_1'`. | Page-view metric fires on HomePage render with the correct application and practice id. | Page view metric. In scope: V2 page view payload. Out of scope: navbar; LiveAgent; nav sidebar. |
| 47 | | does not inject the LiveAgent script if ENABLE_LIVE_AGENT is off | Salesforce LiveAgent script is not injected when AB flag is off. | Render with AB flag off -> assert no #forethought-widget-embed-script elements exist. | LiveAgent script is not injected when the feature flag is disabled. | LiveAgent flag off. In scope: script absence. Out of scope: flag on; mobile. |
| 48 | | does not inject the LiveAgent script if ENABLE_LIVE_AGENT is on for mobile | LiveAgent script is excluded on mobile even with flag on. | Mock useIsSmallBreakpoint returning true -> render with flag on -> assert no script injected. | LiveAgent script is not injected on mobile viewports even when the feature flag is enabled. | LiveAgent mobile exclusion. In scope: mobile check; script absence. Out of scope: desktop; flag off. |
| 49 | | injects the LiveAgent script if ENABLE_LIVE_AGENT is on | LiveAgent script is injected on desktop with flag on. | Render with flag on (non-mobile) -> run pending timers -> assert exactly 1 #forethought-widget-embed-script element exists. | LiveAgent script is injected on desktop when the feature flag is enabled. | LiveAgent injection. In scope: script presence on desktop. Out of scope: mobile; flag off. |
| 49a | | hides the legacy Navbar when PROVGRO_GLOBAL_NAV is on | When the `provgro_global_nav` AB flag is on, the legacy Navbar is hidden and the new provider shell is enabled. | Stub `window.location` to a calendar pathname -> render HomePage with `provgro_global_nav: 'on'` -> assert `provider-shell` has `data-enabled="true"` -> assert `navbar-desktop-view` is not in document. | Legacy Navbar is hidden and provider shell takes over when PROVGRO_GLOBAL_NAV is on. | Global nav sidebar (flag on). In scope: provider-shell enabled state; legacy navbar absent. Out of scope: flag off; LiveAgent. |
| 49b | | shows the legacy Navbar when PROVGRO_GLOBAL_NAV is off | When `provgro_global_nav` is off, the legacy Navbar renders and the provider shell is disabled. | Render HomePage with `provgro_global_nav: 'off'` -> assert `provider-shell` has `data-enabled="false"` -> assert `navbar-desktop-view` is in document. | Legacy Navbar is preserved when PROVGRO_GLOBAL_NAV is off. | Global nav sidebar (flag off). In scope: provider-shell disabled; legacy navbar present. Out of scope: flag on; LiveAgent. |

## PageView-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 50 | | renders an error page with no provider locations | Error page shown when provider has no locations. | Override props with provider having no locations and empty locations array -> render PageView -> assert no-provider-locations-error-page visible -> assert calendar-page-container absent. | Error page renders when a provider has no associated locations. | No locations error. In scope: error page display; no calendar. Out of scope: no providers; normal rendering. |
| 51 | | renders an error page with no providers | Error page shown when providers array is empty. | Override props with empty providers array -> render -> assert error page visible and calendar absent. | Error page renders when no providers are available. | No providers error. In scope: error page display. Out of scope: no locations; normal rendering. |
| 52 | | renders the CalendarPageContainer on desktop viewport | Calendar renders normally on desktop (non-small) viewports. | Mock `useIsSmallBreakpoint` returning false -> render PageView -> assert no-provider-locations-error-page absent -> assert calendar-page-container visible. | Calendar page renders on desktop viewport. | Desktop render. In scope: non-small breakpoint; calendar presence. Out of scope: mobile viewport; error states. |
| 53 | | renders the CalendarPageContainer on mobile viewport | Calendar renders on mobile (small breakpoint) viewports. | Mock `useIsSmallBreakpoint` returning true -> render PageView -> assert no error page -> assert calendar-page-container visible. | Calendar page also renders on mobile viewport. | Mobile render. In scope: small breakpoint; calendar presence. Out of scope: desktop; error states. |

## PatientResultsPage-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 54 | | calls useGetSearchPatients with Marketplace source only when AB flag is off | When the AB flag is off, search hook is invoked with only the Marketplace appointment source. | Mock useSelector returning false (flag off) -> render PatientResultPageContainer with search input -> assert `useGetSearchPatients` called with `practiceId`, `patientName`, `limit: 15`, `sources: [AppointmentSource.Marketplace]`. | Patient search hook receives only the Marketplace source when the AB flag is off. | Search hook params (flag off). In scope: hook call sources. Out of scope: results rendering; flag-on path. |
| 54a | | calls useGetSearchPatients with all appointment sources when AB flag is on | When the AB flag is on, search hook is invoked with Marketplace, Api, and AppointmentList sources. | Mock useSelector returning true (flag on) -> render with search input -> assert `useGetSearchPatients` called with `sources: [Marketplace, Api, AppointmentList]`. | Patient search hook expands the source list when the AB flag is on. | Search hook params (flag on). In scope: expanded sources list. Out of scope: results rendering; flag-off path. |
| 55 | | displays correct search results header and patient rows | Search results page shows header, count, and all patient data. | Render with mock patients -> assert "Search results for test" heading -> assert "4 patient matches" count -> iterate all 4 rows checking first name, last name, DOB (MM/DD/YYYY), email, phone, and sex. | Patient results page displays correct header, match count, and all patient details in each row. | Results display. In scope: header; count; all patient fields per row. Out of scope: search params; navigation. |
| 56 | | sends view metric when search results are shown | V2 View metric fires when results page renders. | Render with patients -> assert V2 "Patient search results" View metric sent with patient IDs in additional_properties. | View metric is sent on patient results page render with patient ID list. | View metric. In scope: metric name; patient IDs. Out of scope: click metrics. |
| 57 | | sends metric when clicking on a patient row | V2 Click metric fires on patient row click. | Render -> click first patient row -> assert V2 Click metric with component_name: 'Patient search results', target_name: 'Patient name', and correct patient_id. | Click metric is sent with correct patient ID when clicking a patient row. | Row click metric. In scope: click metric; patient ID. Out of scope: view metric; navigation. |
| 58 | | sends metric when clicking back to calendar button | V2 Click metric fires on "Back to Calendar" click. | Render -> click "Back to Calendar" -> assert V2 Click metric with component_name: 'Patient search results', target_name: 'Back to Calendar', target_type: 'Button'. | Click metric is sent when clicking the Back to Calendar button. | Back button metric. In scope: click metric; button metadata. Out of scope: navigation behavior. |

## ProviderHeader-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 59 | | show expected providers in alphabetical order (Day) | Day view provider headers display in alphabetical order. | Render CalendarPageContainer -> assert 7 provider header cells in alphabetical order: Ammar through Mawunyo. | Provider headers in Day view are sorted alphabetically. | Day view alphabetical sort. In scope: order verification. Out of scope: pagination; Week view. |
| 60 | | does not show pagination when only one page of providers (Day) | No pagination when providers fit on one page in Day view. | Render with exactly 7 providers -> assert no pagination buttons. | Pagination buttons are hidden when all providers fit on a single page. | Day view no pagination. In scope: button absence. Out of scope: multi-page; Week view. |
| 61 | | shows pagination when more than one page of providers (Day) | Pagination buttons appear with more than one page of providers. | Render with full mock set (>7 providers) -> assert both pagination buttons exist. | Pagination buttons are shown when providers exceed one page. | Day view pagination presence. In scope: button presence. Out of scope: navigation behavior. |
| 62 | | navigates back with full pages (Day) | Day view back pagination shows the next alphabetical set (circular wrap). | Click back button -> assert next 7 providers (Elizabeth through Wesley) appear in order. | Day view back pagination wraps around circularly to show the next set of providers. | Day view back pagination (circular). In scope: back click; provider set; wrap-around. Out of scope: forward; Week view. |
| 63 | | navigates forward with full pages (Day) | Day view forward pagination shows providers with circular wrap. | Click forward button -> assert providers Mike, Peri, Wesley, Ammar, Blake, Dana, Elizabeth appear. | Day view forward pagination wraps around circularly. | Day view forward pagination (circular). In scope: forward click; wrap-around. Out of scope: back; Week view. |
| 64 | | show expected providers in alphabetical order (Week) | Week view provider headers display in alphabetical order. | Switch to Week view -> assert 7 provider header cells in alphabetical order. | Provider headers in Week view are sorted alphabetically. | Week view alphabetical sort. In scope: order verification. Out of scope: Day view; pagination. |
| 65 | | does not show pagination when only one page of providers (Week) | No pagination in Week view when all providers fit on one page. | Render with 7 providers in Week view -> assert no pagination buttons. | Pagination is hidden in Week view when all providers fit on one page. | Week view no pagination. In scope: button absence. Out of scope: multi-page. |
| 66 | | shows pagination when more than one page of providers (Week) | Pagination buttons appear in Week view with multiple pages. | Render in Week view with >7 providers -> assert pagination buttons exist. | Pagination buttons appear in Week view when providers exceed one page. | Week view pagination presence. In scope: button presence. Out of scope: navigation. |
| 67 | | does not navigate back when on first page (Week) | Back button has no effect on first page in Week view. | In Week view on first page -> click back -> assert same 7 providers still shown. | Week view back button does nothing when already on the first page (non-circular). | Week view back boundary. In scope: no-op on first page. Out of scope: forward; Day view circular. |
| 68 | | navigates forward when not on last page (Week) | Forward button shows next page in Week view. | In Week view -> click forward once -> assert last 3 providers (Mike, Peri, Wesley). | Week view forward navigation shows the remaining providers on the next page. | Week view forward navigation. In scope: forward click; partial page. Out of scope: back; circular. |
| 69 | | does not navigate forward when on last page (Week) | Forward button has no effect on last page in Week view. | In Week view -> click forward twice -> assert second click has no effect -> same last 3 providers shown. | Week view forward button does nothing when already on the last page. | Week view forward boundary. In scope: no-op on last page. Out of scope: back. |
| 70 | | navigates back when not on first page (Week) | Back button returns to previous page in Week view. | In Week view -> click forward then back -> assert first page (7 providers) restored. | Week view back navigation returns to the previous page of providers. | Week view back navigation. In scope: forward then back; page restoration. Out of scope: first-page boundary. |
| 71 | | sets first provider alphabetically to active by default in week view | Default active provider is the first alphabetically in Week view. | Switch to Week view -> assert Ammar Hamidou has avatar image (active) -> assert Blake does not. | First alphabetical provider is set as active by default in Week view. | Default active provider. In scope: avatar presence/absence. Out of scope: Day view; localStorage. |
| 72 | | loads active provider by from previous session | Active provider is restored from localStorage in Week view. | Seed localStorage with pr_4_elizabeth -> render in Week view -> verify Elizabeth Ford has avatar. | Active provider is restored from localStorage on mount. | Active provider persistence (load). In scope: localStorage read; avatar display. Out of scope: saving; Day view. |
| 73 | | saves active provider for future session | Active provider selection is saved to localStorage. | Verify initial active is Ammar (localStorage) -> click Blake in Week view -> verify localStorage updated to Blake's ID. | Clicking a provider in Week view saves the selection to localStorage. | Active provider persistence (save). In scope: click; localStorage write. Out of scope: load; pagination. |
| 74 | | sets next provider alphabetically to active when active provider is removed and re-added | Removing and re-adding the active provider does not restore it as active. | Uncheck Ammar from provider filter -> verify Blake becomes active -> re-check Ammar -> verify Blake remains active. | When the active provider is filtered out, the next alphabetical provider becomes active and stays active even after the original is re-added. | Active provider filter removal. In scope: filter uncheck/recheck; active switch; non-restoration. Out of scope: pagination. |
| 75 | | when switching pages in day view, first provider on new page is set to active | Day view pagination updates the active provider to the first on the new page. | Click forward pagination twice -> verify localStorage updates to pr_8_mike, then pr_5_joanna. | Paginating in Day view sets the first provider on the new page as active. | Day view pagination + active provider. In scope: forward clicks; localStorage update per page. Out of scope: Week view; back. |
| 76 | | when switching from day to week view, active provider is on the first page | Active provider from Day view page 2 remains active and visible in Week view. | Navigate to page 2 in Day view (active = Mike) -> switch to Week view -> verify Mike is still active and visible. | Switching from Day to Week view preserves the active provider and ensures they're visible. | Day-to-Week view switch + active provider. In scope: view switch; active preservation. Out of scope: Week-to-Day. |
| 77 | | selects another active provider by clicking on their name in week view | Clicking a provider name in Week view makes them active. | Click Blake in Week view -> assert Blake has avatar -> assert localStorage holds Blake's ID -> assert Ammar no longer active. | Clicking a provider name in Week view changes the active provider. | Active provider click selection. In scope: click; avatar change; localStorage update. Out of scope: pagination. |
| 78 | | preserves active provider when switching pages in week view | Active provider is preserved across page navigation in Week view. | Page forward twice in Week view -> verify active provider (Ammar) preserved in localStorage throughout. | Paging in Week view does not change the active provider. | Week view pagination + active preservation. In scope: forward clicks; localStorage check. Out of scope: Day view. |
| 79 | | active provider is on first page of day view when switching from page without active provider on week view | Switching from Week (page without active) to Day shows active provider on page 1. | Active = Ammar (page 1) -> in Week view move to page 2 (Ammar not visible) -> switch to Day view -> assert Ammar still active and is first provider. | Switching from a Week view page that doesn't show the active provider to Day view still shows the active provider on page 1. | Week-to-Day switch with off-page active. In scope: view switch; active on page 1. Out of scope: active on same page. |
| 80 | | when switching from week to day view, active provider is first on current page | Active provider from Week view becomes first in Day view. | In Week view click Elizabeth -> switch to Day view -> assert Elizabeth shown as first provider. | Switching from Week to Day places the active provider first on the current Day view page. | Week-to-Day active positioning. In scope: view switch; first position. Out of scope: pagination after switch. |
| 81 | | maintains active provider across multiple view switches | Active provider persists through Day -> Week -> Day -> Week toggles. | Switch Day -> Week -> Day -> Week -> verify localStorage keeps Ammar active throughout. | Active provider is maintained across multiple view mode switches. | Multi-switch active preservation. In scope: 4 view switches; localStorage consistency. Out of scope: provider clicks between switches. |
| 82 | | maintains active provider across multiple view switches with updates | Active provider updates are preserved across view switches. | Page forward in Day (Mike active) -> switch to Week -> click Peri -> switch to Day -> verify Peri is active. | Active provider updates during view switches are correctly preserved when switching back. | Multi-switch with active updates. In scope: pagination; click; view switches; final active state. Out of scope: single-direction switch. |

## ProviderLocationFilters-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 83 | | fires metric when auto-selecting and deselecting providers | Auto-select/deselect metrics fire when provider is toggled by location filter. | Set up 2 providers each at one location -> uncheck location lo_1 -> assert V2 Auto-deselect metric for pr_1 -> recheck lo_1 -> assert Auto-select metric for pr_1. | Metrics fire when providers are automatically selected/deselected due to location filter changes. | Provider auto-select/deselect metrics. In scope: location toggle; provider metric. Out of scope: manual provider toggle. |
| 84 | | fires metric when auto-selecting and deselecting locations | Auto-select/deselect metrics fire when location is toggled by provider filter. | Same 2-provider setup -> uncheck provider pr_1 -> assert Auto-deselect metric for lo_1 -> recheck pr_1 -> assert Auto-select metric for lo_1. | Metrics fire when locations are automatically selected/deselected due to provider filter changes. | Location auto-select/deselect metrics. In scope: provider toggle; location metric. Out of scope: manual location toggle. |
| 85 | | renders in person and virtual locations sections | Both "In person" and "Virtual" sections render with correct locations. | Render with both in-person and virtual locations -> assert "In person" and "Virtual" section labels -> check test IDs for in-person (lo_1-lo_4) and virtual (lo_5). | Location filters render separate "In person" and "Virtual" sections with correct location entries. | Location section rendering (both). In scope: section labels; location test IDs. Out of scope: single section; interactions. |
| 86 | | renders in person locations section only | Only "In person" section renders when no virtual locations exist. | Render with only in-person locations -> assert "In person" label present -> assert no "Virtual" section. | Only "In person" section renders when there are no virtual locations. | In-person only section. In scope: section presence/absence. Out of scope: virtual only; interactions. |
| 87 | | renders virtual locations section only | Only "Virtual" section renders when no in-person locations exist. | Render with only virtual locations -> assert no "In person" section -> assert "Virtual" label present. | Only "Virtual" section renders when there are no in-person locations. | Virtual only section. In scope: section presence/absence. Out of scope: in-person only. |
| 88 | | all filters are selected by default | All provider and location checkboxes are checked by default. | Render -> assert every input in both Providers and Locations settings sections is checked. | All provider and location filters default to selected. | Default filter state. In scope: all checkboxes checked. Out of scope: localStorage; interactions. |
| 89 | | loads filters from previous session | Filter selections are restored from localStorage. | Seed localStorage with specific provider and location filter selections -> render -> verify each checkbox reflects stored selection -> verify correct providers in header row (first 7) -> verify location checkboxes match stored selection. | Provider and location filter state is correctly restored from localStorage. | Filter persistence load. In scope: localStorage read; checkbox states; header row. Out of scope: saving; interactions. |
| 90 | | unchecking a provider removes them from the provider header | Unchecking a provider filter removes them from the calendar header. | Uncheck Blake Zhai -> assert checkbox unchecked -> assert Blake removed from provider header row. | Unchecking a provider filter immediately removes them from the calendar header. | Provider uncheck -> header removal. In scope: checkbox uncheck; header update. Out of scope: rechecking; location filters. |
| 91 | | checking a provider adds back to the provider header | Re-checking a provider filter adds them back to the calendar header. | Uncheck then re-check Blake Zhai -> assert checkbox checked -> assert Blake reappears in header. | Re-checking a provider filter restores them in the calendar header. | Provider recheck -> header restore. In scope: checkbox recheck; header update. Out of scope: unchecking; location filters. |
| 92 | | saves filters for future session | Filter changes are persisted to localStorage. | Uncheck 3 providers and 2 locations -> read localStorage -> verify stored providerIds and locationIds reflect deselected items. | Provider and location filter changes are correctly saved to localStorage for future sessions. | Filter persistence save. In scope: localStorage write; correct IDs. Out of scope: loading; interactions beyond uncheck. |

## ZoomLevel-tests.tsx

| # | Do we need these? | Test Name | What It Tests | Steps | Summary | Scope |
|---|-------------------|-----------|---------------|-------|---------|-------|
| 93 | | sets default zoom level to compact | Default zoom level is "Compact" when no preference exists. | Render CalendarPageContainer -> open zoom dropdown -> assert selected option is "Compact". | Calendar defaults to Compact zoom level. | Default zoom. In scope: default selection display. Out of scope: localStorage; size changes. |
| 94 | | preserves zoom level from previous session | Zoom level is restored from localStorage. | Seed localStorage with 'default' zoom -> render -> open dropdown -> assert selected option is "Default". | Zoom level preference is correctly restored from localStorage. | Zoom persistence (load). In scope: localStorage read; selection display. Out of scope: saving; size changes. |
| 95 | | selects zoom level from toggle and persists in local storage | Changing zoom level updates the dropdown and persists to localStorage. | Render -> open dropdown -> click "Default" -> re-open dropdown -> assert "Default" selected -> verify localStorage equals 'default'. | Zoom level change is reflected in the dropdown and persisted to localStorage. | Zoom selection + persistence. In scope: dropdown click; localStorage write. Out of scope: size changes. |
| 96 | | changes time slot and provider header height when zoom level changes | Zoom level change updates CSS heights for time slots and provider headers. | Render -> assert Compact slot wrapper height = 44px and provider header cell = 48px -> switch to Default -> assert both are 64px. | Zoom level change adjusts time slot and provider header heights. | Zoom CSS effect. In scope: slot height; header height per level. Out of scope: persistence; dropdown UI. |
| 97 | | does not change zoom level if selected option clicked | Clicking the already-selected zoom option is a no-op. | Start at Compact (44px slots) -> open dropdown -> click "Compact" again -> assert dropdown closes -> assert slot height stays 44px. | Clicking the already-selected zoom level is a no-op. | Zoom no-op selection. In scope: re-click; no height change; dropdown close. Out of scope: actual level change. |
